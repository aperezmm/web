const withTM = require('next-transpile-modules')(['@devperez/components'])

module.exports = withTM()

//NEXT JS

/*
Tanto el codigo del frontend como el del backend se ejecutan simultaneamente. (Isomorfico)
Como FULLSTACK tener en cuenta lo que ocurre en Javascript cliente y Javascript lado servidor.
*Es una librería
*Soporta estilos globales en pages/_app.js

*Ideal para varios components, soluciona problema de routing,
*/ 
